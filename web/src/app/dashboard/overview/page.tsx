/**
 * Route entry for the overview parallel layout.
 *
 * The slot pages render the current dashboard panels; this page provides the
 * required route entry so both Next production and development servers resolve
 * /dashboard/overview consistently.
 */
export default function OverviewPage() {
  return null;
}
