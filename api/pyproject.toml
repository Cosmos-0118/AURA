[project]
name = "aura-api"
version = "0.1.0"
description = "AURA marketing pipeline API"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic",
  "pydantic-settings",
  "psycopg[binary]",
  "pymysql",
  "httpx",
  "python-dotenv",
  "python-multipart",
  "openai",
  "google-genai>=2.24.0",
  "fal-client>=1.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["main.py", "db.py", "schemas.py", "graph.py", "routes", "agents"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-mock>=3.15.1",
]
